{
  "name": "covid",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil App Starter",
  "scripts": {
    "build": "stencil build",
    "deploy": "npm run build && surge ./www stencil-d3-dashboard.surge.sh",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "^1.12.2",
    "@stencil/router": "^1.0.1",
    "@types/jest": "24.9.1",
    "@types/puppeteer": "2.0.1",
    "jest": "24.9.0",
    "jest-cli": "24.9.0",
    "jest-fetch-mock": "^3.0.3",
    "prettier": "2.0.5",
    "puppeteer": "2.1.1"
  },
  "license": "MIT",
  "dependencies": {
    "d3": "^5.16.0"
  }
}
